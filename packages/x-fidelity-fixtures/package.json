{
  "name": "x-fidelity-fixtures",
  "version": "1.0.0",
  "private": true,
  "description": "Test fixtures for all x-fidelity archetypes - each archetype directory contains intentionally problematic code to exercise every rule",
  "workspaces": {
    "packages": [
      "node-fullstack",
      "java-microservice"
    ],
    "nohoist": [
      "**/node-fullstack/**",
      "**/java-microservice/**"
    ]
  },
  "scripts": {
    "build": "echo 'Build is intentionally a noop for fixtures - this is a test repository'",
    "test": "node scripts/validate-all-archetypes.js",
    "test:all": "node scripts/validate-all-archetypes.js",
    "coverage:matrix": "node scripts/generate-coverage-matrix.js",
    "clean": "yarn workspaces run clean",
    "reset:all": "yarn workspaces run reset",
    "lint": "echo 'No linting for fixtures - this code is intentionally problematic'"
  },
  "devDependencies": {
    "chalk": "^4.1.2",
    "glob": "11.0.3"
  },
  "engines": {
    "node": ">=22.16.0",
    "yarn": ">=1.22.0"
  }
} 