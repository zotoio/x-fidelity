{
  "name": "@x-fidelity/typescript-config",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "files": ["base.json", "node.json", "vscode.json"],
  "scripts": {
    "build": "echo 'Build noop for typescript-config - using configs directly'",
    "clean": "echo 'Clean noop for typescript-config'",
    "dev": "echo 'Dev noop for typescript-config'",
    "test": "echo 'No tests for typescript-config package'",
    "test:unit": "echo 'No tests for typescript-config package'",
    "test:coverage": "echo 'No tests for typescript-config package'",
    "lint": "echo 'No linting for typescript-config package'",
    "lint:fix": "echo 'No linting for typescript-config package'",
    "check-types": "echo 'Type checking noop for typescript-config - no TypeScript files'"
  }
} 