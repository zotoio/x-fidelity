{
  "name": "x-fidelity-monorepo",
  "version": "4.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/x-fidelity-vscode",
      "**/x-fidelity-vscode/**",
      "**/x-fidelity-fixtures",
      "**/x-fidelity-fixtures/**"
    ]
  },
  "scripts": {
    "clean": "yarn workspaces run clean && rm -rf dist *.tsbuildinfo",
    "clean:artifacts": "find . -name '*.d.ts' -o -name '*.map' -o -name '*.js.map' | grep -v node_modules | grep -v dist | xargs rm -f || true",
    "build:clean": "yarn clean && yarn clean:artifacts && yarn build",
    "build": "yarn workspaces run build",
    "build:watch": "tsc -b --watch",
    "test": "yarn workspaces run test",
    "test:coverage": "yarn workspaces run test:coverage",
    "test:coverage:report": "yarn test:coverage && yarn coverage:merge",
    "coverage:merge": "nyc merge coverage .nyc_output/coverage.json && nyc report --reporter=html --reporter=text --reporter=lcov",
    "coverage:check": "nyc check-coverage --statements 80 --branches 75 --functions 80 --lines 80",
    "coverage:detailed": "yarn test:coverage && echo '\n📊 COVERAGE SUMMARY BY PACKAGE:' && find packages -name 'coverage' -type d -exec echo 'Package: {}' \\; -exec cat {}/lcov-report/index.html \\; 2>/dev/null || true",
    "lint": "yarn workspaces run lint",
    "lint:fix": "yarn workspaces run lint:fix",
    "commit": "yarn test && git-cz",
    "build-run": "yarn build && yarn workspace x-fidelity run build-run",
    "vscode:dev": "yarn workspace x-fidelity-vscode dev",
    "vscode:dev:fresh": "yarn workspace x-fidelity-vscode dev:fresh",
    "vscode:dev:watch": "yarn workspace x-fidelity-vscode dev:watch",
    "vscode:package": "yarn workspace x-fidelity-vscode package",
    "test:consistency": "yarn build && cd packages/x-fidelity-core && npm test -- --testPathPatterns=consistencyTesting.test.ts --verbose",
    "test:consistency:quick": "yarn build && cd packages/x-fidelity-core && node -e \"require('./dist/test-utils/consistencyTesting.js').runManualConsistencyTest().catch(console.error)\"",
    "test:consistency:baseline": "yarn build && cd packages/x-fidelity-core && node -e \"require('./dist/test-utils/consistencyTesting.js').generateBaselineReport().then(r => require('fs').writeFileSync('../../consistency-baseline.txt', r))\"",
    "test:consistency:validate": "yarn build && cd packages/x-fidelity-core && node dist/test-utils/consistencyTestingCLI.js validate"
  },
  "dependencies": {
    "@types/node": "22",
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "c8": "^10.1.3",
    "commitizen": "^4.3.1",
    "cz-conventional-changelog": "^3.3.0",
    "istanbul-merge": "^2.0.0",
    "nyc": "^17.1.0",
    "typescript": "^5.8.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
