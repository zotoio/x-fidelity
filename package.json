{
  "name": "x-fidelity-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/x-fidelity-cli",
      "packages/x-fidelity-core", 
      "packages/x-fidelity-democonfig",
      "packages/x-fidelity-fixtures",
      "packages/x-fidelity-plugins",
      "packages/x-fidelity-server",
      "packages/x-fidelity-types",
      "packages/x-fidelity-vscode"
    ],
    "nohoist": [
      "**/x-fidelity-vscode",
      "**/x-fidelity-vscode/**",
      "**/x-fidelity-fixtures",
      "**/x-fidelity-fixtures/**"
    ]
  },
  "scripts": {
    "clean": "yarn workspaces run clean",
    "clean:artifacts": "find . -name '*.d.ts' -o -name '*.map' -o -name '*.js.map' | grep -v node_modules | grep -v dist | xargs rm -f || true",
    "build:clean": "yarn clean && yarn build",
    "build": "yarn workspace @x-fidelity/types build && yarn workspace @x-fidelity/core build && yarn workspace @x-fidelity/plugins build && yarn workspace @x-fidelity/democonfig build && yarn workspace @x-fidelity/server build && yarn workspace x-fidelity build && yarn workspace x-fidelity-vscode build",
    "test": "yarn workspaces run test:unit",
    "test:unit": "yarn workspaces run test:unit",
    "test:integration": "yarn workspace x-fidelity test:integration && yarn workspace x-fidelity-vscode test:integration",
    "test:all": "yarn test:unit && yarn test:integration",
    "ci": "yarn test:unit && yarn test:integration && yarn workspace x-fidelity global:test:local",
    "test:coverage": "yarn workspaces run test:coverage",
    "test:coverage:report": "yarn test:coverage && yarn coverage:merge",
    "coverage:merge": "nyc merge coverage .nyc_output/coverage.json && nyc report --reporter=html --reporter=text --reporter=lcov",
    "coverage:check": "nyc check-coverage --statements 80 --branches 75 --functions 80 --lines 80",
    "coverage:detailed": "yarn test:coverage && echo '\n📊 COVERAGE SUMMARY BY PACKAGE:' && find packages -name 'coverage' -type d -exec echo 'Package: {}' \\; -exec cat {}/lcov-report/index.html \\; 2>/dev/null || true",
    "lint": "yarn workspaces run lint",
    "lint:fix": "yarn workspaces run lint:fix",
    "commit": "yarn test && git-cz",
    "build-run": "yarn build && yarn workspace x-fidelity run build-run",
    "vscode:dev": "yarn workspace x-fidelity-vscode dev",
    "vscode:dev:fresh": "yarn workspace x-fidelity-vscode dev:fresh",
    "vscode:dev:watch": "yarn workspace x-fidelity-vscode dev:watch",
    "vscode:package": "yarn workspace x-fidelity-vscode package",
    "cli:build": "yarn workspace x-fidelity build"
  },
  "dependencies": {
    "@types/node": "22",
    "ajv": "^8.17.1",
    "depcheck": "^1.4.7"
  },
  "devDependencies": {
    "c8": "^10.1.3",
    "commitizen": "^4.3.1",
    "cz-conventional-changelog": "^3.3.0",
    "istanbul-merge": "^2.0.0",
    "nyc": "^17.1.0",
    "typescript": "^5.8.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}